# Backend
BACKEND_PORT=3001
DATABASE_URL=./data/messages.db

# Gateway
GATEWAY_PORT=3002
GATEWAY_URL=http://localhost:3002

# Queue configuration
SEND_INTERVAL_MS=3600000   # 1 hour in milliseconds (configurable via API)
MESSAGES_PER_TICK=1         # Messages sent per interval
